[package]
name = "hawk2ui-platform"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Capability-scoped platform APIs for filesystem, network, clipboard, secrets, and database access in Hawk2UI."

[lib]
name = "hawk2ui_platform"
path = "src/lib.rs"

[lints]
workspace = true
