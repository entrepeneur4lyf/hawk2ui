[package]
name = "hawk2ui-platform"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Capability-scoped platform APIs for filesystem, network, clipboard, secrets, AI, audio, dialogs, localization, MCP, notifications, shortcuts, and database access in Hawk2UI."

[lib]
name = "hawk2ui_platform"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
jsonschema = { version = "0.46.5", default-features = false }
schemars = "1.2.1"
serde.workspace = true
serde_json = "1"
ureq = "3.3.0"
url.workspace = true
